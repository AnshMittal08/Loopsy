{
  "name": "crochet-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p ${PORT:-3000}",
    "test": "node --import ./test/setup.mjs --test test/*.test.js",
    "backup": "node scripts/backup-db.js",
    "migrate": "node scripts/migrate.js",
    "migrate:deploy": "node scripts/migrate.js --allow-skip",
    "smoke:pg": "node --import ./test/setup.mjs test/pg-smoke.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.107.0",
    "better-sqlite3": "^12.11.1",
    "next": "^16.2.9",
    "pg": "^8.22.0",
    "react": "^19",
    "react-dom": "^19",
    "stripe": "^22.3.0",
    "uuid": "^14.0.1",
    "zod": "^4.4.3"
  }
}
